// use gamepad_api::GamepadAPI;
// use std::thread::sleep;
// use std::time;

fn main() {
    // let mut api = GamepadAPI::new();
    // loop {
    //     let gamepads = api.get_gamepads();
    //     sleep(time::Duration::from_millis(50));
    // }
}
